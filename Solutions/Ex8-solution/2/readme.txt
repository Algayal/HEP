For installation run: make 
To run the program: make events
./test.job 
Then run the python code, incase of error, change the file paths (commented part)
Idealy, one could write one c++ code utilizing root a pythia, but I ran into problems with the Makefile.